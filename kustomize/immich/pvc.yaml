apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-longhorn
  namespace: applications
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Gi
